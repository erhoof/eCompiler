{
    int i;
    int j;

    write("Check numbs for greatiness: ");

    i = 30;
    j = 20;

    write("i equals: ");
    write(i);
    write("j equals: ");
    write(j);

    if (i > j) {
        write("i is greater than j");
    }

    if (j > i) {
        write("j is greater than i");
    }
}